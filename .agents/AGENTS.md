<RULE[startup_and_context.md]>
# Startup & Context Rules

- **Token Optimization**: On startup, ALWAYS apply the `markdown_token_optimizer` skill to reduce token usage (there are some awesome skills agents that need to be used).
- **Context Retrieval**: ALWAYS use `graphify` to get context for any codebase or architectural questions.
</RULE[startup_and_context.md]>
